<Window x:Class="Maverick_ObfuSQF_Windows_Interface.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Maverick_ObfuSQF_Windows_Interface"
        Title="Bytex Digital - ObfuSQF" Height="180" MinHeight="180" Width="400" MinWidth="400"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <DockPanel Grid.Row="0">
      <Label Margin="10,10,10,10">
        Please provide your login token
      </Label>
    </DockPanel>
    <DockPanel Grid.Row="1" LastChildFill="false">
      <TextBox Name="tbToken" Margin="10,10,10,10" Height="30" DockPanel.Dock="Top"
               VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
    </DockPanel>
    <DockPanel Grid.Row="2" LastChildFill="false">
      <Button Click="btnLogin_Click" Name="btnLogin" Margin="10,10,10,10" MinWidth="100" MinHeight="25"
              DockPanel.Dock="Right">Apply Key</Button>
    </DockPanel>
  </Grid>
</Window>